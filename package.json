{
  "name": "green_map",
  "version": "1.0.0",
  "description": "Prima versione dell'applicazione GreenMap",
  "main": "server.js",
  "scripts": {
    "build-all": "npm install && cd app && npm install && npm run build",
    "build-dev": "npm install && cd app && npm install && npm run build-test",
    "start": "node server.js",
    "dev": "node -r dotenv/config server.js",
    "test": "jest --setupFiles dotenv/config"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "geolib": "^3.3.4",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.14.1",
    "nodemailer": "^7.0.3"
  },
  "devDependencies": {
    "dotenv": "^16.5.0",
    "jest": "^29.7.0",
    "supertest": "^7.1.1"
  },
  "jest": {
    "verbose": true,
    "collectCoverage": false
  }
}
